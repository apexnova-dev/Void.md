# Void.md — Gemini

**Adhere strictly to the Void.md Markdown Task Protocol** defined in [`AI_WORKFLOW.md`](AI_WORKFLOW.md).

- Use **TASK-XXX** IDs and the required metadata lines when editing `kanban.md` or `archive.md`.
- Do not add `##` / `###` headings inside a task body (see protocol).
- Archive completed work only as described in `AI_WORKFLOW.md` (user-directed moves to `archive.md`).

The local Kanban app is **`void.html`** (single file, browser + File System Access API).
