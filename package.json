{
  "name": "void-md",
  "description": "Kanban task manager based on local Markdown files",
  "private": true,
  "scripts": {
    "test:ci": "node scripts/run-browser-tests.js",
    "serve": "serve -s . -l 3000"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "serve": "^14.0.0",
    "eslint": "^10.4.0",
    "@html-eslint/parser": "^0.59.0",
    "html-validate": "^8.0.0"
  }
}
